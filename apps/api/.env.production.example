# COMMUNE API - Production Environment Template
# Use this file as a reference for Railway/Render/Fly.io/etc. secret values.
# Do NOT commit real credentials.

NODE_ENV=production
PORT=3000

# Frontend URL (exact deployed web URL)
FRONTEND_URL=https://your-frontend-domain.com

# Database (Neon)
# Runtime app traffic (recommended: Neon pooler)
DATABASE_URL=postgresql://<user>:<password>@<pooler-host>/<db>?sslmode=verify-full

# Optional: use direct Neon endpoint for migration jobs if required by your platform
MIGRATION_DATABASE_URL=postgresql://<user>:<password>@<direct-host>/<db>?sslmode=verify-full

# Keep DB_* only for local DNS-bypass troubleshooting. Leave empty in production.
DB_HOST=
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=
DB_SSL=true
DB_ENDPOINT=
DB_SERVERNAME=

# JWT
JWT_SECRET=<generate-a-long-random-secret>
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Redis
REDIS_HOST=<redis-host>
REDIS_PORT=6379
REDIS_PASSWORD=<redis-password>

# OAuth - Google
GOOGLE_CLIENT_ID=<google-client-id>
GOOGLE_CLIENT_SECRET=<google-client-secret>
GOOGLE_CALLBACK_URL=https://api.your-domain.com/api/v1/auth/oauth/google/callback

# OAuth - GitHub
GITHUB_CLIENT_ID=<github-client-id>
GITHUB_CLIENT_SECRET=<github-client-secret>
GITHUB_CALLBACK_URL=https://api.your-domain.com/api/v1/auth/oauth/github/callback
